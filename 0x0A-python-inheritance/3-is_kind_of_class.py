#!/usr/bin/python3
""" 3-is_kind_of_clas """


def is_kind_of_class(obj, a_class):
    """ if object belongs to or is an instance of the class return else false """
    return isinstance(obj, a_class)
